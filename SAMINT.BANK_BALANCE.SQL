CREATE TABLE SAMINT.BANK_BALANCE (
 BBNUMBER INT UNSIGNED NOT NULL AUTO_INCREMENT,
 BNAME VARCHAR(75),
 ANUMBER VARCHAR(45), 
 CURRENCY VARCHAR(3), 
 DATEOFENTRY DATE,
 BALANCE BIGINT UNSIGNED, 
 PRIMARY KEY (BBNUMBER)
);