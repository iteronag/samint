CREATE TABLE SAMINT.MATERIAL (
 MNUMBER INT UNSIGNED NOT NULL AUTO_INCREMENT,
 MDESC VARCHAR(75), 
 ATTR VARCHAR(45),
 UOM VARCHAR(45), 
 DPRICE INT(25),
 STOCKINHAND INT(15), 
 PRICE INT(25), 
 CURRENCY VARCHAR(3),
 PRIMARY KEY (MNUMBER)
);